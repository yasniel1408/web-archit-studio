export const miniMapStyles = {
  container:
    "absolute bottom-4 right-4 z-50 glass-panel rounded-xl shadow-xl overflow-hidden flex flex-col fade-in",
  header:
    "text-gray-700 text-xs px-3 py-2 border-b border-border/20 flex justify-between items-center",
  headerTitle: "font-medium",
  emptyText: "text-gray-400 text-xs",
  canvas: "bg-white/70",
  emptyMessageContainer: "absolute inset-0 flex items-center justify-center text-gray-400 text-xs",
};
